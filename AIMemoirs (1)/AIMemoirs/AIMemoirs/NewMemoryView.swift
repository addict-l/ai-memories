import SwiftUI

struct NewMemoryView: View {
    var body: some View {
        VStack {
            Text("新的回忆页面")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    NewMemoryView()
} 