import SwiftUI

struct ProfileView: View {
    var body: some View {
        VStack {
            Text("我的页面")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    ProfileView()
} 